@model LMS.Models.ViewModels.TareasViewModel
@using LMS.Extensions


@using LMS.Utility
@{
    ViewData["Title"] = "Tareas";

    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<br />
<br />

<div class="border backgroundWhite">

    <div class="row">

        <div class="col-6">

            <h2 class="text-info">Lista de Tareas</h2>
        </div>

        @if (User.IsInRole(SD.TeacherUser))
        {
            //editar esto
            @*<div class="col-6 text-right">

                <partial name="_CreateTareaButtonPartial" />
            </div>*@
            <div class="col-6 text-right">
    <p>

        @*<a asp-action="CrearTarea" asp-route="@Model.Curso.Id" class="btn btn-info"><i class="fas fa-plus"></i>&nbsp; Crear Nuevo</a>*@

        <a href="@Url.Action("CrearTarea", "Tareas", new { Id = Model.Curso.Id})" class="btn btn-info"><i class="fas fa-plus"></i>&nbsp; Crear Nuevo</a>
    </p>
            </div>
        }




    </div>


    <br />


    <div>
        @if (Model.TareasList.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.Tareas.NombreTarea)

                    </th>

                    <th>



                        @Html.DisplayNameFor(m => m.Tareas.Cursos.Nombre)
                    </th>

                    <th>

                        @Html.DisplayNameFor(m => m.Tareas.Materia.Nombre)


                    </th>

                    <th>
                

                    </th>

                    <th>
                  

                    </th>



                    <th>

                    </th>

                    <th>


                    </th>


                </tr>

                @foreach (var item in Model.TareasList)
                {
                    <tr>

                        <td>

                            @Html.DisplayFor(m => item.NombreTarea)


                        </td>

                        <td>
                            @Html.DisplayFor(m => item.Cursos.Nombre)

                        </td>

                        <td>

                            @Html.DisplayFor(m => item.Materia.Nombre)
                        </td>

                        <td>
                           

                        </td>

                        <td>
                         

                        </td>

                        @if (User.IsInRole(SD.TeacherUser))
                        {

                            //editar esto
                            <td>
                                <partial name="_CursoEditButtonsPartial" model="item.Id" />
                            </td>

                        }
                    </tr>

                }
            </table>
        }
        else
        {
            <p> no hay Tareas existentes...</p>
        }
    </div>


</div>



